// Copyright 2021 NNTU-CS

int cbinsearch(int *arr, int size, int value) {
  int count = 0;
    for (int a = 0; a <= size; a++) {
        if (*(arr + i) == value)
            count += 1;
    }
    return count;
  return 0;
}
